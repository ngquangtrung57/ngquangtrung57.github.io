import { defineConfig } from 'astro/config';

// User/org GitHub Pages site → served at the domain root, no `base` needed.
export default defineConfig({
  site: 'https://ngquangtrung57.github.io',
});
